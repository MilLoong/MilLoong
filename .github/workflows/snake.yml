name: Generate GitHub Snake

on:
  schedule:
    # 每天世界协调时间 (UTC) 0点运行一次
    # 如果你想每天在不同时间运行，可以修改这里的 cron 表达式
    - cron: "0 0 * * *"
  workflow_dispatch:
  
jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      # 🐍 使用 Platane/snk Action 生成动画
      - name: Generate Snake Animation
        uses: Platane/snk@v3 
        with:
          # 必填：你的 GitHub 用户名
          github_user_name: ${{ github.repository_owner }} 
          
          # 必填：输出文件的路径和名称 (使用 SVG 格式，比 GIF 更清晰)
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark 

          # 可选：如果你的主页是黑色的，可以使用这个主题
          # color_theme: dark
          
      # 🚀 将生成的文件推送到仓库
      - name: Push to Repository
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: main # 推送到主分支
          build_dir: dist # 指定包含生成的 SVG 的目录
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # 使用内置的 token